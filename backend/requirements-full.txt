fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.1
pydantic-settings==2.1.0
sqlalchemy==2.0.23
alembic==1.13.1
PyMuPDF==1.23.14
numpy==1.24.3
pandas==2.1.4
python-multipart==0.0.6
openpyxl==3.1.2
reportlab==4.0.7
celery==5.3.4
redis==5.0.1
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Advanced PDF Processing & Computer Vision - PERMANENTLY DISABLED
# 
# REASON FOR REMOVAL:
# These libraries were causing memory pressure (1GB+ AI models) leading to process death
# after idle periods. Our current workflow uses simple coordinate math instead of AI:
# - Users manually select areas with mouse
# - PDFScaleCalculator converts coordinates to real dimensions  
# - No computer vision needed for this workflow
#
# DISABLED LIBRARIES:
# opencv-python==4.8.1.78    # Computer vision (edge detection, image processing)
# pytesseract==0.3.10        # Tesseract OCR text recognition
# easyocr==1.7.0             # AI-powered OCR (includes PyTorch models)
# scikit-image==0.22.0       # Image processing algorithms
# scipy==1.11.4             # Scientific computing
# matplotlib==3.8.2         # Data visualization
# seaborn==0.13.0           # Statistical visualization  
# scikit-learn==1.3.2       # Machine learning algorithms
#
# DISABLED ENDPOINTS:
# /api/pdf/analyze-advanced          # Advanced PDF analysis with CV
# /api/pdf/detect-joists-advanced    # AI joist detection
# /api/pdf/auto-populate-advanced    # OCR form population
#
# CURRENT ACTIVE ENDPOINT:
# /api/pdf/calculate-dimensions      # Math-only coordinate conversion

# Keep Pillow for basic image processing if needed later
Pillow==10.1.0

# Claude Vision API - LEGACY (not used in current workflow)
# anthropic==0.39.0

# System monitoring - REMOVED (not needed)
# psutil